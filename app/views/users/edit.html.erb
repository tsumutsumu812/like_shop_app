<h1>ユーザー編集画面</h1>

<%= render partial: "form", locals: { user: @user } %>
        
<%if @user.id == current_user.id || current_user.admin?%>
  <%= link_to "アカウント削除", @user, class:"btn btn-danger", method: :delete, data: {confirm: "削除してもよろしいですか？"} %>
<%end%>